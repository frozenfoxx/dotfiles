- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    # Shell
    ~/.aliases: shell/.aliases
    ~/.zshrc: shell/.zshrc

    # Git
    ~/.gitconfig: git/.gitconfig

    # Tmux
    ~/.tmux.conf: tmux/.tmux.conf

    # Vim
    ~/.vimrc: vim/.vimrc

- create:
    - ~/Documents/repos

- create:
    ~/.ssh:
      mode: 0700

- shell:
  - [bash requirements.sh, Checking requirements]
  - [git submodule update --init --recursive, Installing submodules]

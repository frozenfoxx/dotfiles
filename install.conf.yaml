- defaults:
    link:
      create: true
      relink: true

- clean: ['~']

- link:
    # Shell
    ~/.zshrc: shell/.zshrc

    # Git
    ~/.gitconfig: git/.gitconfig

    # Tmux
    ~/.tmux.conf: tmux/.tmux.conf

    # Vim
    ~/.vimrc: vim/.vimrc

- create:
    - ~/Documents/repos

- create:
    ~/.ssh:
      mode: 0700

- shell:
  - [sh requirements.sh, Checking requirements]
  - [git submodule update --init --recursive, Installing submodules]
